PROMPT 1 — AUDIT شامل لصفحة /import (بدون تغييرات)

انسخ ده كما هو:

PROMPT — PURCH-IMPORT-UNIQUE-AUDIT-1 | AUDIT ONLY | NO CHANGES | RAW PROOFS | STOP

ROLE: Senior Full-Stack Engineer
MODE: AUDIT ONLY (NO CODE CHANGES)

OUTPUT MUST BE: RAW FILE SNIPPETS + RAW COMMAND OUTPUTS ONLY (NO NARRATIVE).

Confirm Excel headers for uploaded file

Print the ordered header list A→AG and mark blank headers positions (Z/AA/AB/AD/AE).

Show how ImportPage.tsx maps columns to fields (snippet: the mapping function / row parser).

Trace the actual write pipeline

Show the exact lines where forbidDirectWrite is called during import (all occurrences in ImportPage.tsx).

Show atomicWriteGuard.tsx forbidDirectWrite implementation (the throw).

Identify the intended RPC path

Show how the app is SUPPOSED to write items (which RPC should be called) vs what it calls now.

List all RPCs called by ImportPage.tsx with line numbers.

DB readiness checks (psql)
Paste outputs:

\d+ jewelry_items

\d+ purchase_batches

\df+ purchase_invoice_import_unique_atomic

\df+ import_purchase_batch_create_atomic

STOP.

PROMPT 2 — FIX DB فقط (إضافة JSONB أعمدة حفظ “الهيدرز + الصف + الإضافات”)

ده يحقق مطلب “الهيدرز كلها موجودة في DB” بدون تفريغ سكيمة بأعمدة Z/AA… إلخ.

PROMPT — PURCH-IMPORT-UNIQUE-DB-JSONB-2 | DB ONLY | MIGRATION + PROOFS | STOP

ROLE: Database Engineer
MODE: DB ONLY (SQL ONLY)

STRICT:

Allowed: CREATE/ALTER + CREATE OR REPLACE FUNCTION فقط

Forbidden: UI edits, server/routes.ts edits

GOAL:
Add 3 columns to jewelry_items to preserve Excel data exactly (including blank headers):

raw_row_json jsonb (entire row as received, ordered)

raw_headers_json jsonb (ordered headers array length = number of columns)

extra_fields_json jsonb (positional extras for columns beyond the “known mapped” set)

STEPS:

Create migration file: migrations/purch_import_unique_jsonb_cols.sql

ALTER TABLE jewelry_items ADD COLUMN IF NOT EXISTS ... for the 3 columns

Add safe defaults: NULL allowed

Prove with:

\d+ jewelry_items (show new columns)

OUTPUT REQUIRED:
A) git diff --stat
B) git diff --
C) Full migration contents pasted
D) psql proof output

STOP.

PROMPT 3 — FIX الاستيراد (Atomic RPC للكتابة بدل forbidDirectWrite) + تحقق End-to-End

ده هو اللي “يخلي الشاشة شغالة فعلاً” ويضمن: إنشاء قطع + فاتورة + قيود + حركات مخزون.

PROMPT — PURCH-IMPORT-UNIQUE-ATOMIC-WRITE-3 | MIN CHANGE | RPC + UI WIRE | PROOFS | STOP

ROLE: Senior Full-Stack Engineer
MODE: MINIMAL FIX (ATOMIC RPC ONLY) + Small UI wiring if needed

STRICT:

No direct DB writes from UI (no supabase/from/insert).

All writes must go through ONE atomic RPC.

Keep VAT option present but default VAT=0 for import items unless explicitly provided.

GOAL (Unique-only, NO jewelry_sets usage):

Disable/remove ALL jewelry_sets calls from ImportPage.tsx.

Import writes must treat every row as a unique item.

Serial/item_code generated by system (existing sequences).

Persist ALL Excel columns:

Map known fields into existing jewelry_items columns

Store the entire ordered row into raw_row_json

Store ordered headers into raw_headers_json

Store extras into extra_fields_json (positional)

IMPLEMENTATION:

Create/Replace RPC:

purchase_invoice_import_unique_atomic (or a new one: purchase_import_unique_excel_atomic)
that:

Validates supplier_id, branch_id, supp_inv uniqueness

Creates purchase_batch (or attaches to existing batch)

Inserts jewelry_items rows

Creates purchase invoice + lines

Creates item_movements

Creates journal entry + lines (inventory/AP)

VAT lines are optional; default 0, but structure supports VAT later

UI:

Replace the forbidDirectWrite loop with a single dataGateway.rpc call to the atomic RPC

Keep UI behavior, only change the write call

PROOFS REQUIRED:
A) git diff --stat
B) git diff --
C) Curl proof (authenticated) to the RPC showing success
D) DB proofs after run:

latest invoice row (id, invoice_number, supplier_invoice_no, status)

count(*) jewelry_items inserted for that invoice

count(*) item_movements created

JE balance proof (sum debit = sum credit)
E) Verify visibility:

Query jewelry_items returned by the products list API / selectors (show a table-read proof)

STOP.
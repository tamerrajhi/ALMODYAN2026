✅ PROMPT — NEW USER CAN’T LOGIN (INVALID CREDENTIALS) | DB + CODE PROOF + FIX | NO GUESSING | STOP

ROLE: Full-stack Engineer (Express + Postgres + React)
MODE: Evidence-first. Print raw outputs. Then minimal fix only.

GOAL:
Fix: user created from /users cannot login (API returns "invalid credentials").

SECTION A — SHOW CURRENT LOGIN LOGIC (RAW)
A1) Open server/auth.ts (or wherever /api/auth/login is implemented).
Paste the entire login handler block (verbatim), including:
- which table it queries
- which field it uses (email_or_username, email, username)
- bcrypt.compare usage and what column it compares against

SECTION B — SHOW CURRENT CREATE USER LOGIC (RAW)
B1) Locate Users create endpoint in server/routes.ts (POST /api/users or similar).
Paste the entire handler (verbatim), including:
- which table it inserts into
- which fields it writes (email, username, password/password_hash)
- whether it hashes password with bcrypt before insert

SECTION C — DB PROOF (RAW SQL OUTPUT)
Run these queries and print results:

C1) Show last 5 users from the SAME table login reads:
SELECT id, email, username, created_at,
       CASE WHEN password_hash IS NULL THEN 'NULL' ELSE LEFT(password_hash, 7) END AS pw_prefix
FROM profiles
ORDER BY created_at DESC
LIMIT 5;

(If login reads a different table, run the same query on that table.)

C2) Find the newly created user row by email or username:
SELECT id, email, username, created_at,
       CASE WHEN password_hash IS NULL THEN 'NULL' ELSE LEFT(password_hash, 7) END AS pw_prefix
FROM profiles
WHERE email = '<NEW_EMAIL>' OR username = '<NEW_USERNAME>'
LIMIT 5;

C3) Confirm uniqueness constraints / duplicates:
SELECT email, COUNT(*) FROM profiles GROUP BY email HAVING COUNT(*) > 1;
SELECT username, COUNT(*) FROM profiles GROUP BY username HAVING COUNT(*) > 1;

SECTION D — FIX (MINIMAL, CORRECT)
D1) If create endpoint is storing password without hashing OR password_hash is NULL:
- Update create endpoint to require a password input
- Hash it using bcrypt (same salt rounds used elsewhere)
- Store into password_hash column
- Ensure login reads from the same table and compares to password_hash

D2) If create endpoint inserts into a different table than login reads:
- Fix create to insert into the login table OR update login to read from the correct table

EVIDENCE REQUIRED
E1) git diff --stat
E2) git diff (only touched files)
E3) After fix: create a user, then login with that user (show raw HTTP result/status).
STOP.

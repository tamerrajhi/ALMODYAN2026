PROMPT — ACC-BRANCH-COA-TREEFIX-VAT-1 | MINIMAL SAFE | DB + PROVISION | EVIDENCE | STOP

ROLE: Senior Full-Stack Engineer (Replit + Neon + Express)
MODE: MINIMAL CHANGES (DB-first). No UI refactor.

GOAL:
1) Fix Branch COA orphans by setting correct parent_id so BRxx-* accounts are nested under the existing global tree.
2) Introduce a clean long-term VAT header node (global) and re-parent VAT accounts under it.
3) Update provision_branch_coa_atomic so future branch provisioning sets parent_id correctly (including VAT under VAT header).

STRICT CONSTRAINTS:
- Do NOT change any existing account_code values.
- Do NOT delete accounts.
- Only create ONE new global header account (VAT header) if it doesn't exist.
- All updates must be idempotent and safe (re-runnable).
- Tree logic is parent_id only.

ASSUMPTIONS TO VERIFY (must print DB output first):
- chart_of_accounts has columns: id, account_code, name_ar, account_type, parent_id, is_system
- Existing parents exist: 2000, 1100, 1200, 2100, 4000, 5000

STEP A — DB VERIFICATION (RAW OUTPUT)
Paste outputs:
1) \d+ chart_of_accounts
2) SELECT account_code, id FROM chart_of_accounts WHERE account_code IN ('2000','1100','1200','2100','4000','5000','2105','2110') ORDER BY account_code;
3) SELECT account_code, id, parent_id FROM chart_of_accounts WHERE account_code LIKE 'BR01-%' ORDER BY account_code;

STEP B — MIGRATION FILE (IDEMPOTENT)
Create: migrations/acc_branch_coa_treefix_vat_1.sql

B1) Create global VAT header (only if not exists):
- account_code: '2090'
- name_ar: 'ضريبة القيمة المضافة'
- account_type: liability
- parent_id: id of '2000'
- is_system: true
Use INSERT ... SELECT ... WHERE NOT EXISTS.

B2) Re-parent global VAT accounts under 2090 (safe):
- UPDATE chart_of_accounts SET parent_id = (id of 2090) WHERE account_code IN ('2105','2110') AND (parent_id IS DISTINCT FROM (id of 2090));

B3) Fix existing branch orphans for ALL branches (safe):
- For any account_code like 'BR%-%' OR 'MAIN-%' with parent_id IS NULL:
  set parent_id using mapping by suffix AFTER the dash:
  - 110101 -> parent '1100'
  - 110104 -> parent '1100'
  - 1301   -> parent '1200'
  - 2101   -> parent '2100'
  - 2105   -> parent '2090'   (VAT header)
  - 2110   -> parent '2090'   (VAT header)
  - 4100   -> parent '4000'
  - 5100   -> parent '5000'
Only update when the parent exists and account_code matches.

B4) Update provision_branch_coa_atomic:
- Ensure newly created branch accounts are inserted with correct parent_id based on the SAME mapping above.
- Specifically: VAT branch accounts must use parent_id = 2090 id (VAT header), not 2000.
- Must remain idempotent (re-running provisioning for same branch should not duplicate accounts; should preserve/repair parent_id).

STEP C — APPLY MIGRATION + PROOFS (RAW OUTPUT)
Evidence required:
A) git diff --stat
B) git diff --
C) After-proof for VAT structure:
- SELECT a.account_code, p.account_code AS parent_code
  FROM chart_of_accounts a
  LEFT JOIN chart_of_accounts p ON p.id = a.parent_id
  WHERE a.account_code IN ('2090','2105','2110','BR01-2105','BR01-2110')
  ORDER BY a.account_code;

D) After-proof for BR01 nesting:
- SELECT a.account_code, p.account_code AS parent_code
  FROM chart_of_accounts a
  LEFT JOIN chart_of_accounts p ON p.id = a.parent_id
  WHERE a.account_code LIKE 'BR01-%'
  ORDER BY a.account_code;

E) Child counts under key parents for BR01:
- SELECT parent.account_code AS parent_code, count(*) AS child_count
  FROM chart_of_accounts child
  JOIN chart_of_accounts parent ON parent.id = child.parent_id
  WHERE child.account_code LIKE 'BR01-%'
  GROUP BY parent.account_code
  ORDER BY parent.account_code;

F) Provision idempotency check (no duplicates):
- Execute the existing provision function for BR01 (or whichever is used) and show:
  1) It does not insert duplicate BR01-* rows.
  2) It keeps/repairs parent_id correctly.
Paste the exact call used and its output.

G) npx tsc --noEmit (exit 0)

STOP after all raw outputs.

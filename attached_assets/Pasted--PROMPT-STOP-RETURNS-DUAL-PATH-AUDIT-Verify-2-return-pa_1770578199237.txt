✅ PROMPT — STOP-RETURNS-DUAL-PATH-AUDIT | Verify 2 return paths: POS Piece Return vs General Return | EVIDENCE ONLY | STOP

ROLE: Auditor (Fullstack)

MODE: AUDIT ONLY — NO FIXES, NO CODE CHANGES

GOAL
Confirm the system has two return paths:
A) POS Piece Return (unique items, POS)
B) General Return (non-POS / general invoices)
and map exactly how each path works end-to-end.

EVIDENCE REQUIRED

1) UI ROUTE MAP (Exact)
List the return-related pages/routes and their purpose:
- src/pages/POSReturnPage.tsx
- src/pages/sales/SalesReturnsPage.tsx
- src/pages/sales/SalesReturnViewPage.tsx
- src/pages/ReturnsPage.tsx
- Any other return pages discovered (search "Return", "مرتجع", "credit note")

For each page provide:
- Route path
- Which return type it represents (POS piece vs general)
- Primary actions (search, load items, confirm return)

2) DATA CALLS PER PAGE (Exact)
For each page above, provide a table:
- dataGateway.rpc calls (function names + payload keys)
- dataGateway.queryTable calls (table names + filters)
- fetch() endpoints (URL + method)

Paste the exact code blocks for:
- The "confirm/complete return" action handler
- The API call lines

3) BACKEND ENDPOINTS / RPC DISPATCH (Exact)
Identify backend handlers used by return flows:
- Express routes: /api/sale-items, /api/sale-items-by-barcode, and any /api/returns* endpoints
- RPC names: complete_pos_piece_return_atomic, complete_pos_credit_note_atomic, any "sales_return" RPCs

Paste full handler code blocks from server/routes.ts for each used endpoint.

4) DB TABLES USED (Exact)
For each return path, list which tables are written/updated:
- POS Piece Return: expected to touch sales/returns/payments/journal_entries/unique_items
- General Return: identify actual tables (returns tables, invoices, invoice items, etc.)

Provide SQL proof by reading schema (no writes):
- List tables matching '%return%' and '%credit%' and '%refund%':
  SELECT table_name FROM information_schema.tables
  WHERE table_schema='public' AND (table_name ILIKE '%return%' OR table_name ILIKE '%credit%' OR table_name ILIKE '%refund%')
  ORDER BY table_name;

For key tables found, dump columns:
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema='public' AND table_name='<table>'
ORDER BY ordinal_position;

5) CONSISTENCY CHECKS (Critical)
Answer with evidence:
- Does POSReturnPage currently rely on legacy sale_items? (Yes/no + proof)
- Does POS Piece Return update unique_items.sold_at/sale_id back to NULL? (find in RPC/function or server code)
- Do general returns affect unique_items at all, or are they invoice-level only?

STOP
After delivering sections 1-5 evidence, STOP. No fixes.

✅ PROMPT — STOP-ACC-VERIFY-P0P1 | Verify claimed Accounting changes | EVIDENCE ONLY | NO NEW CHANGES | STOP

ROLE: Auditor

MODE: EVIDENCE ONLY — ABSOLUTELY NO CHANGES
- No new commits
- No SQL DDL
- No function edits
- No refactors
- Only: show evidence (git diffs + DB proofs + endpoint proofs + gate test outputs)

CONTEXT
You previously claimed:
- 7 DB functions created (5 JE + 2 COA)
- Constraints/indexes/FKs added
- POST /api/chart-of-accounts removed/converted to atomic
- requireSession applied to accounting endpoints
- Frontend wired to atomic RPCs instead of forbidDirectWrite

Your claims are NOT accepted without RAW evidence.

========================
A) GIT EVIDENCE (MANDATORY)
========================
A1) git diff --stat against the last known baseline commit that existed BEFORE your changes.
- If you don't know baseline, show:
  git log --oneline -n 15
  and identify commits that contain your changes.

A2) git diff FULL (no truncation) for:
- server/routes.ts
- src/pages/accounting/JournalEntriesPage.tsx
- src/pages/accounting/ChartOfAccountsPage.tsx
- any new/changed helper modules you touched

A3) Prove forbidDirectWrite removal/absence:
- grep results (paste raw output):
  grep -R "forbidDirectWrite" -n src/pages/accounting/JournalEntriesPage.tsx src/pages/accounting/ChartOfAccountsPage.tsx

STOP if any of the above cannot be produced.

========================
B) DB FUNCTION PROOFS (MANDATORY)
========================
For EACH of the 7 functions (exact names):
- create_journal_entry_atomic
- post_journal_entry_atomic
- unpost_journal_entry_atomic
- reverse_journal_entry_atomic
- update_journal_entry_atomic
- update_chart_of_account_atomic
- delete_chart_of_account_atomic

Run and paste:
B1) Existence + signature:
SELECT proname, pg_get_function_identity_arguments(p.oid) AS args
FROM pg_proc p
JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public' AND proname IN (
'create_journal_entry_atomic','post_journal_entry_atomic','unpost_journal_entry_atomic',
'reverse_journal_entry_atomic','update_journal_entry_atomic',
'update_chart_of_account_atomic','delete_chart_of_account_atomic'
)
ORDER BY proname;

B2) Full function body (NO TRUNCATION):
SELECT pg_get_functiondef(p.oid)
FROM pg_proc p
JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public' AND proname = '<FUNCTION_NAME>';

Repeat B2 for all 7.

========================
C) DB DDL PROOFS (CONSTRAINTS/INDEXES/FKs)
========================
C1) journal_entries.status CHECK constraint proof:
SELECT conname, pg_get_constraintdef(c.oid) AS def
FROM pg_constraint c
JOIN pg_class t ON c.conrelid = t.oid
JOIN pg_namespace n ON t.relnamespace = n.oid
WHERE n.nspname='public' AND t.relname='journal_entries' AND c.contype='c'
ORDER BY conname;

C2) Unique index on credit_notes.credit_note_number proof:
SELECT indexname, indexdef
FROM pg_indexes
WHERE schemaname='public' AND tablename='credit_notes'
ORDER BY indexname;

C3) Unique invoice_number proof (show exactly what was added):
SELECT indexname, indexdef
FROM pg_indexes
WHERE schemaname='public' AND tablename='invoices'
ORDER BY indexname;

C4) sales_invoice_items FK proofs:
SELECT conname, pg_get_constraintdef(c.oid) AS def
FROM pg_constraint c
JOIN pg_class t ON c.conrelid = t.oid
JOIN pg_namespace n ON t.relnamespace = n.oid
WHERE n.nspname='public' AND t.relname='sales_invoice_items' AND c.contype='f'
ORDER BY conname;

C5) Performance index proofs (journal_entry_lines, journal_entries, payments):
SELECT tablename, indexname, indexdef
FROM pg_indexes
WHERE schemaname='public'
  AND tablename IN ('journal_entry_lines','journal_entries','payments')
ORDER BY tablename, indexname;

========================
D) SERVER ROUTES + AUTH GUARD PROOFS
========================
D1) Show the actual code blocks in server/routes.ts for:
- POST /api/chart-of-accounts  (prove it is NOT a raw INSERT)
- GET /api/chart-of-accounts (prove requireSession applied)
- GET /api/journal-entries (requireSession)
- GET /api/journal-entries/:id OR /api/journal-entry-detail (whatever exists) (requireSession)
- GET /api/payments (requireSession)
- GET /api/invoices (requireSession)

D2) Runtime proof with curl (paste raw):
- Unauthed => 401 JSON:
  curl -i http://localhost:<PORT>/api/journal-entries
- Authed => 200 JSON:
  (use a valid session cookie from your environment)
  curl -i --cookie "__session=<token>" http://localhost:<PORT>/api/journal-entries

If you cannot produce an authenticated token, STOP and say "BLOCKER: no session token".

========================
E) GATE TESTS (EVIDENCE ONLY)
========================
Run minimal DB-only gate tests (no code changes):
E1) Create draft JE via create_journal_entry_atomic (provide JSON result only)
E2) Post balanced JE => success + status posted
E3) Post imbalanced JE => fail with error_code (show JSON)
E4) Reverse posted JE => new JE created and linked (show JSON)
E5) Update posted JE => must fail (draft-only rule) (show JSON)

STOP after A–E evidence. No fixes. No suggestions. No commentary.

MODE: IMPLEMENTATION (SAFE, READ-ONLY). Build POS Admin dashboard page at /pos/pos-dashboard and keep /pos/reports as detailed reports.

CONSTRAINTS
- No DB schema changes in this step.
- Read-only endpoints only.
- All dashboard/report endpoints must be protected by requirePosAdminToken.
- Do not touch ERP /api/admin/* endpoints.
- Use existing validated linkages:
  sales_invoice_items.jewelry_item_id -> unique_items.id
  return_items.item_id -> unique_items.id
  returns.original_sale_id -> sales.id (derive seller for returns)
  seller_profile_id exists in sales/invoices/payments

TASKS
1) ROUTE + NAV
- Add a POS Admin route: /pos/pos-dashboard
- Add a sidebar/nav item (POS Admin only): "لوحة التحكم" -> /pos/pos-dashboard
- Keep existing "التقارير" -> /pos/reports

2) DASHBOARD CONFIG (NO DB)
Create file: src/config/posDashboard.ts
Export a widgets config array like:
[
  { key: "today_kpis", title: "ملخص اليوم", enabled: true },
  { key: "profit_snapshot", title: "الأرباح", enabled: true },
  { key: "top_sellers", title: "أفضل البائعين", enabled: true },
  { key: "top_branches", title: "أفضل الفروع", enabled: true },
  { key: "inventory_valuation", title: "تقييم المخزون", enabled: true },
  { key: "inventory_aging", title: "عمر المخزون", enabled: true },
  { key: "reconciliation", title: "الفروقات", enabled: true }
]
UI must render only enabled widgets.

3) FILTERS (UI)
At top of dashboard:
- Quick ranges: Today / Last 7 days / This month
- Custom: start_date, end_date (ISO)
All widgets that depend on dates must respect the selected range.

4) ENDPOINTS (READ-ONLY)
Add new endpoints under /api/pos/admin/dashboard/* (all requirePosAdminToken):
- GET /api/pos/admin/dashboard/today-kpis?start=&end=&branch_id=
  Returns: sales_count, revenue, tax, discounts, net_sales, returns_amount

- GET /api/pos/admin/dashboard/profit-snapshot?start=&end=&branch_id=&seller_id=
  Returns: revenue, cogs, gross_profit, return_amount, return_cost, net_profit, gp_percent

- GET /api/pos/admin/dashboard/top-sellers?start=&end=&branch_id=&limit=5
  Returns list: seller_id, seller_name, revenue, cogs, net_profit

- GET /api/pos/admin/dashboard/top-branches?start=&end=&limit=5
  Returns list: branch_id, branch_name, revenue, cogs, net_profit

- GET /api/pos/admin/dashboard/inventory-valuation?branch_id=
  Returns: item_count, total_cost_value, total_tag_value, potential_margin grouped by branch

- GET /api/pos/admin/dashboard/inventory-aging?branch_id=
  Returns counts by buckets based on unique_items.created_at:
   >30d, >60d, >90d and total_in_stock

- GET /api/pos/admin/dashboard/reconciliation?start=&end=&branch_id=&limit=50
  Compare invoices.total_amount vs SUM(payments.amount) where payments.reference_type='invoice' and payments.reference_id=invoices.id
  Return rows where delta != 0 with: invoice_number, branch, seller, invoice_total, paid_total, delta

All endpoints must respond using the existing convention:
- Success: { data: {...}, error: null }
- Failure: status 500 { data: null, error: { message: "..." } }

5) UI WIDGETS
Create page component: src/pages/pos/POSDashboardPage.tsx (or equivalent)
- Render cards/widgets matching config keys.
- Each widget has a "تفاصيل" link to /pos/reports with tab and date params.

6) VERIFICATION
- Without __pos_admin cookie: all /api/pos/admin/dashboard/* return 401 {error:"POS admin session required"}
- With valid POS admin: all endpoints return 200.
- Dashboard loads and shows data; no changes to DB schema; no changes to /api/admin/*.

DELIVERABLE
- Files changed list
- Endpoint list + SQL used (brief)
- Proof of 401 vs 200
- Confirm: no schema changes and ERP endpoints untouched
PROMPT — UNIQUE-PURCH-MODULE-V1 | AUDIT → BUILD | STRICT EVIDENCE | STOP-GATED

ROLE: Senior Full-Stack Engineer
MODE: Phased execution with hard STOP gates.

RULES (STRICT):

Do NOT guess any schema. If missing, query DB and paste outputs.

Jewelry_sets must be removed from the unique import path completely.

VAT default is 0 but vat_rate option must exist; only post VAT lines if vat_rate > 0.

Duplicate policy ONLY on SUPP INV per supplier (supports multi-file). Enforce via DB partial unique index + RPC precheck.

Excel data must be stored as-is including empty headers and empty columns, preserving original column order. Store as:

raw_headers_json (jsonb array) — include "" for empty headers

raw_values_json (jsonb array) — include "" for empty cells

raw_row_json optional (jsonb)

Serial/unique item code is generated by system during import; downstream operations by serial.

========================
STOP-1 (AUDIT ONLY — NO CHANGES)

Paste raw outputs:

\d+ public.journal_entries

\d+ public.journal_entry_lines

\d+ public.item_movements

\d+ public.branches

\d+ public.suppliers

\d+ public.atomic_workflow_requests

Account codes existence (MAIN + one more branch if exists):
Run SQL and paste outputs:
SELECT account_code, account_name
FROM chart_of_accounts
WHERE account_code IN ('1301','2101','2105','1100','1200','2000','2090')
OR account_code LIKE 'MAIN-%'
ORDER BY account_code;

Code proof (snippets only):

grep -n "jewelry_sets" src/pages/ImportPage.tsx

grep -n "forbidDirectWrite" src/pages/ImportPage.tsx

grep -n "purchase_invoice_supp_inv_precheck" src/pages/ImportPage.tsx

grep -n "purchase_invoice_import_unique_atomic" src/pages/ImportPage.tsx
STOP after showing outputs.

========================
STOP-2 (DB ONLY — CREATE UNIQUE TABLES + VIEW)

Create ONE migration:
migrations/unique_purch_module_v1.sql

It must create tables:

unique_purchase_batches

unique_purchase_invoices

unique_purchase_invoice_items

unique_items

unique_purchase_returns

unique_purchase_return_items

Core columns (minimum):
unique_purchase_invoices:

supplier_id, branch_id, batch_id

supp_inv text

invoice_number text

invoice_date date/timestamptz

status text (posted/voided/etc)

vat_rate numeric NOT NULL DEFAULT 0

subtotal numeric NOT NULL DEFAULT 0

tax_amount numeric NOT NULL DEFAULT 0

total_amount numeric NOT NULL DEFAULT 0

journal_entry_id uuid NULL

created_at, created_by

unique_items:

serial_no text UNIQUE NOT NULL

branch_id uuid, supplier_id uuid, batch_id uuid, unique_invoice_id uuid NULL

stockcode, model, description, tag_price, cost, division, type, metal, clarity, etc (as needed)

sale_id uuid NULL, sold_at timestamptz NULL

raw_headers_json jsonb NOT NULL DEFAULT '[]'::jsonb (MUST be array)

raw_values_json jsonb NOT NULL DEFAULT '[]'::jsonb (MUST be array)

raw_row_json jsonb NULL

created_at, created_by

Constraints / Indexes:

Partial unique index on (supplier_id, upper(trim(supp_inv))) WHERE status <> 'voided' AND supp_inv is not null AND trim(supp_inv)<>''.

unique(serial_no) on unique_items.

FKs between batches→invoices→invoice_items→items, and returns→return_items.

Create view v_sellable_unique_items selecting:
id, serial_no, stockcode, model, description, tag_price,
(CASE WHEN sale_id IS NULL AND sold_at IS NULL THEN 'sellable' ELSE 'not_sellable' END) AS sellable_status,
sold_at, branch_id

Evidence (raw):

cat migrations/unique_purch_module_v1.sql

psql apply output

\d+ each new table + the view
STOP.

========================
STOP-3 (DB ONLY — ATOMIC RPCs)

Create migration:
migrations/unique_purch_module_v1_rpcs.sql

Create functions:

unique_purchase_supp_inv_precheck(p_supplier_id uuid, p_supp_invs text[]) returns jsonb

unique_purchase_import_excel_atomic(p_payload jsonb) returns jsonb

unique_purchase_return_create_atomic(p_payload jsonb) returns jsonb

unique_purchase_return_void_atomic(p_payload jsonb) returns jsonb

Import requirements:

p_payload has: client_request_id, supplier_id, branch_id, vat_rate(default 0),
files:[{supp_inv, invoice_date, rows:[{raw_headers_json(jsonb array), raw_values_json(jsonb array), raw_row_json(jsonb)}]}]

Generate serial_no via a dedicated sequence unique_serial_seq (create if missing).

Insert unique_items preserving raw_headers_json/raw_values_json EXACTLY (including empty strings and order).

Create invoices + invoice_items links + item_movements purchase_in referencing unique_invoice_id and unique_item_id.

Create JE:

Debit inventory: <BR>-1301 else 1301

Credit AP: <BR>-2101 else 2101 else 2100

VAT lines only if vat_rate > 0: Debit VAT input <BR>-2105 else 2105

Idempotency: atomic_workflow_requests using (client_request_id, workflow_type='unique_import_excel'), cache result_payload.

Evidence:

cat migrations/unique_purch_module_v1_rpcs.sql

psql apply output

\df+ each function signature
STOP.

========================
STOP-4 (SERVER — ALLOWLIST ONLY)

Add to RPC_ALLOWLIST in server/routes.ts:

unique_purchase_supp_inv_precheck

unique_purchase_import_excel_atomic

unique_purchase_return_create_atomic

unique_purchase_return_void_atomic

Evidence:

git diff --stat

git diff --
STOP.

========================
STOP-5 (UI — WIRE IMPORT PAGE ONLY)

Modify ONLY src/pages/ImportPage.tsx:

Remove jewelry_sets calls/paths completely.

Remove/disable forbidDirectWrite insert loop.

Fix precheck call to use body: { args: { p_supplier_id, p_supp_invs } }

On import: call /api/rpc/unique_purchase_import_excel_atomic with body:
{ args: { p_payload: ... } }

Keep multi-file behavior: one invoice per file, SUPP INV per file.

Display summary (batch + invoices + counts + errors).
No other page edits.

Evidence:

git diff --stat

git diff --

tsc exit code
STOP.

========================
STOP-6 (PROOFS — CURL + DB)

Paste exact RPC endpoint path from server/routes.ts (the Express route line).

CURL precheck (authenticated) with 2 supp invs.

CURL import (authenticated) with 1 file, 2 rows, vat_rate=0, and raw_headers_json/raw_values_json arrays (include "" entries).

Paste SQL outputs:

SELECT batch_no, status, rows_total, rows_imported FROM unique_purchase_batches ORDER BY created_at DESC LIMIT 3;

SELECT invoice_number, supp_inv, status, total_amount, journal_entry_id FROM unique_purchase_invoices ORDER BY created_at DESC LIMIT 3;

SELECT serial_no, stockcode, model, tag_price, sale_id, sold_at FROM unique_items ORDER BY created_at DESC LIMIT 5;

SELECT entry_number, total_debit, total_credit FROM journal_entries WHERE id = '<latest_je_id>';

SELECT account_id, debit_amount, credit_amount FROM journal_entry_lines WHERE journal_entry_id = '<latest_je_id>' ORDER BY created_at;

SELECT reference_type, reference_id FROM item_movements ORDER BY created_at DESC LIMIT 5;

STOP after raw outputs only.